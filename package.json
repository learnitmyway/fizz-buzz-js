{
  "name": "fizz-buzz-js",
  "private": true,
  "scripts": {
    "lint": "npx eslint --config .eslintrc.json 'src/**/*.js' --max-warnings 0",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@babel/core": "^7.10.1",
    "@babel/preset-env": "^7.10.1",
    "babel-jest": "^26.0.1",
    "eslint": "^7.1.0",
    "eslint-plugin-jest": "^23.13.2",
    "jest": "^26.0.1"
  }
}
